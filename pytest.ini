[pytest]
testpaths = tests
markers =
    api: API-level tests using FastAPI TestClient or websocket endpoints.
    integration: Cross-component tests (database, persistence, bot, runtime wiring).
    unit: Pure unit tests for isolated logic.
addopts = --strict-markers
